name: Build CastTV APK

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # 1. 自动补全“启动钥匙”
      - name: Regenerate Gradle Wrapper
        run: gradle wrapper --gradle-version 8.5

      # 2. 强行注入 AndroidX 全局开关
      - name: Enable AndroidX in gradle.properties
        run: |
          echo "android.useAndroidX=true" >> gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties

      # 3. 稳健修复：使用 100% 干净不拦截的公共镜像中心（阿里云源/Gradle官方公共节点）下载真实的 `.jar` 文件
      - name: Download Cling Jars Securely
        run: |
          mkdir -p app/libs
          
          # 下载 cling-core (优先从稳定的公开插件仓库或国内高速阿里云中心拉取二进制流)
          curl -L -A "Mozilla/5.0" -o app/libs/cling-core-2.1.2.jar https://plugins.gradle.org/m2/org/fourthline/cling/cling-core/2.1.2/cling-core-2.1.2.jar
          
          # 下载 cling-support
          curl -L -A "Mozilla/5.0" -o app/libs/cling-support-2.1.2.jar https://plugins.gradle.org/m2/org/fourthline/cling/cling-support/2.1.2/cling-support-2.1.2.jar
          
          # 验证下载下来的包是不是真实的非空包（检查大小是否正常）
          echo "--- 检查下载的本地依赖依赖包大小 ---"
          ls -lh app/libs/
          
          # 如果包大小小于 10KB，说明被远端用 HTML 网页糊弄了，我们用备用高速节点重新覆盖
          if [ $(stat -c%s "app/libs/cling-core-2.1.2.jar") -lt 10000 ]; then
            echo "触发备用节点下载..."
            curl -L -o app/libs/cling-core-2.1.2.jar https://maven.aliyun.com/repository/public/org/fourthline/cling/cling-core/2.1.2/cling-core-2.1.2.jar
            curl -L -o app/libs/cling-support-2.1.2.jar https://maven.aliyun.com/repository/public/org/fourthline/cling/cling-support/2.1.2/cling-support-2.1.2.jar
            ls -lh app/libs/
          fi

      # 4. 强制项目依赖本地整个 libs 目录下的有效 Jar 包
      - name: Patch Build Gradle to use Local Jars
        run: |
          if [ -f "app/build.gradle.kts" ]; then
            sed -i '/org.fourthline.cling/d' app/build.gradle.kts
            sed -i '/dependencies {/a \    implementation(fileTree(mapOf("dir" to "libs", "include" to listOf("*.jar"))))' app/build.gradle.kts
          fi

      # 5. 保持上一步 100% 成功的完美复写布局结构
      - name: Re-write activity_main.xml perfectly
        run: |
          LAYOUT_FILE="app/src/main/res/layout/activity_main.xml"
          mkdir -p app/src/main/res/layout
          
          cat << 'EOF' > $LAYOUT_FILE
          <?xml version="1.0" encoding="utf-8"?>
          <androidx.constraintlayout.widget.ConstraintLayout 
              xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#F5F5F7">

              <TextView
                  android:id="@+id/tvTitle"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:text="红果短剧投屏助手"
                  android:textSize="20sp"
                  android:textStyle="bold"
                  android:textColor="#333333"
                  android:layout_marginTop="28dp"
                  app:layout_constraintTop_toTopOf="parent"
                  app:layout_constraintStart_toStartOf="parent"
                  app:layout_constraintEnd_toEndOf="parent" />

              <Button
                  android:id="@+id/btnStartService"
                  android:layout_width="0dp"
                  android:layout_height="wrap_content"
                  android:text="启动监听服务"
                  android:layout_marginHorizontal="32dp"
                  android:layout_marginTop="32dp"
                  app:layout_constraintTop_toBottomOf="@id/tvTitle"
                  app:layout_constraintStart_toStartOf="parent"
                  app:layout_constraintEnd_toEndOf="parent" />

              <Button
                  android:id="@+id/btnScanDevices"
                  android:layout_width="0dp"
                  android:layout_height="wrap_content"
                  android:text="扫描电视设备"
                  android:layout_marginHorizontal="32dp"
                  android:layout_marginTop="16dp"
                  app:layout_constraintTop_toBottomOf="@id/btnStartService"
                  app:layout_constraintStart_toStartOf="parent"
                  app:layout_constraintEnd_toEndOf="parent" />

              <Button
                  android:id="@+id/btnStopCast"
                  android:layout_width="0dp"
                  android:layout_height="wrap_content"
                  android:text="停止投屏"
                  android:layout_marginHorizontal="32dp"
                  android:layout_marginTop="16dp"
                  app:layout_constraintTop_toBottomOf="@id/btnScanDevices"
                  app:layout_constraintStart_toStartOf="parent"
                  app:layout_constraintEnd_toEndOf="parent" />

              <TextView
                  android:id="@+id/tvInstructions"
                  android:layout_width="0dp"
                  android:layout_height="wrap_content"
                  android:text="使用说明：\n1. 点击&quot;启动监听服务&quot;\n2. 打开红果短剧播放视频\n3. 自动提取视频URL后点击&quot;扫描设备&quot;\n4. 选择电视设备并开始投屏"
                  android:textSize="13sp"
                  android:textColor="#666666"
                  android:lineSpacingMultiplier="1.3"
                  android:padding="16dp"
                  android:layout_marginHorizontal="32dp"
                  android:layout_marginTop="24dp"
                  app:layout_constraintTop_toBottomOf="@id/btnStopCast"
                  app:layout_constraintStart_toStartOf="parent"
                  app:layout_constraintEnd_toEndOf="parent"
                  app:layout_constraintBottom_toBottomOf="parent" />

          </androidx.constraintlayout.widget.ConstraintLayout>
          EOF
          echo "--- 智能完美复写完成 ---"

       Cinderella-Step:
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 6. 执行正式编译打包
      - name: Build with Gradle
        run: ./gradlew assembleDebug assembleRelease

      # 7. 上传真实的最终生产 APK 文件
      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CastTV-DLNA-APKs
          path: |
            app/build/outputs/apk/debug/*.apk
            app/build/outputs/apk/release/*.apk
